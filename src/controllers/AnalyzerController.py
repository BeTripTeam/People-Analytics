from src.analysis.users.FbUserAnalyzer import FbUserAnalyzer
from src.analysis.users.VKUserAnalyzer import VKUserAnalyzer
from src.models.user.AUser import AUser


class AnalyzerController:
    # analyzers = {
    #     'vk': VKUserAnalyzer(),
    #     'fb': FbUserAnalyzer()}
    #
    # def analyze(self, network_name, user: AUser):
    #     self._get_analyzer(network_name).analyze(user)
    #
    # def analyze_skills(self, network_name, user: AUser):
    #     self._get_analyzer(network_name).analyze_skills(user)
    #
    # def analyze_interests(self, network_name, user: AUser):
    #     self._get_analyzer(network_name).analyze_interests(user)
    #
    # def _get_analyzer(self, network_name):
    #     return self.analyzers.get(network_name)
    pass